# PERN-Backend

Test repo for PERN stack with Docker setup

## To run Docker

```bash
docker-compose up --build
```

## To run postgres during development

```bash
docker-compose -f docker-compose.dev.yaml up -d
```

```

```
